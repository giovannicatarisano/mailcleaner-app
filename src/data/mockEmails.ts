import { EmailAccount, EmailMessage } from '../types/index.ts';

// Stato iniziale pulito per la versione di produzione (nessun account o email fittizia di prova)
export const initialAccounts: EmailAccount[] = [];
export const initialMockEmails: EmailMessage[] = [];
